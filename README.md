# ProckX - Мощный E-commerce Магазин

Современный, быстрый и безопасный онлайн-магазин построенный на React + Node.js + MongoDB.

## 🚀 Возможности

### Frontend
- **React 18** с хуками и контекстом
- **Vite** для молниеносной разработки
- **React Query** для кэширования и синхронизации данных
- **Framer Motion** для плавных анимаций
- **Responsive дизайн** - работает на всех устройствах
- **Lazy loading** компонентов и изображений
- **Protected routes** для аутентификации

### Backend
- **Node.js + Express** RESTful API
- **MongoDB + Mongoose** для данных
- **JWT аутентификация** с обновлением токенов
- **Rate limiting** защита от спама
- **Helmet, CORS, XSS** - полная безопасность
- **Stripe интеграция** для платежей
- **Multer + Sharp** для обработки изображений

### Безопасность
- Защита от XSS, CSRF, NoSQL инъекций
- Хеширование паролей bcrypt
- Rate limiting на всех критичных маршрутах
- CSP заголовки
- Валидация всех входных данных

### Производительность
- React Query кэширование
- Code splitting
- Оптимизированные изображения
- Минимизированные бандлы
- База данных с индексами

## 📦 Установка

```bash
# Клонировать репозиторий
git clone https://github.com/yourusername/prockx.git
cd prockx

# Установить зависимости
cd server && npm install
cd ../client && npm install

# Настроить переменные окружения
# server/.env
# client/.env

# Запустить MongoDB
mongod --dbpath ./data

# Запустить сервер
cd server && npm run dev

# Запустить клиент
cd client && npm run dev